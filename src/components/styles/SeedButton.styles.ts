import styled from 'styled-components/native'
import { Button } from 'react-native-paper'

export const SeedButton = styled(Button)`
    width: auto;
    height: auto;
    border-radius: 20px;
    border-color: #A017B7;
    border-width: 1px;
    margin: 2px;
    
    `
    // ${props => props.onPress && props.key && `visibility: hidden;`}
